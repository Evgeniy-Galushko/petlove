export const selectPrice = (state) => state.filters.price;

export const selectPopularity = (state) => state.filters.popularity;

export const selectRequest = (state) => state.filters.request;

export const selectCategory = (state) => state.filters.category;

export const selectGenders = (state) => state.filters.gender;

export const selectSpecie = (state) => state.filters.specie;

export const selectLocationId = (state) => state.filters.locationId;
